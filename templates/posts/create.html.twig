{% extends 'base.html.twig' %}
{% block body %}
    <h1>Create a new Post</h1>
    <div>
        {{ form_start(postForm) }}
        {{ form_widget(postForm) }}
        <input type="submit" value="Create"/>
        {{ form_end(postForm) }}
    </div>
{% endblock %}